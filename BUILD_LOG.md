# Sleep Light Study — 搭建日志

本文记录网站从需求定义、实验实现到公开部署的主要过程。时区为 Asia/Shanghai。第 1–14 节保留 v1/v2 当时的实现历史；第 15–20 节记录首个 2026-07-18 Protocol v3；第 21–25 节记录双语姓名档案版本；第 26 节起记录 2026-07-23 密码账户、后来取消的邮件提醒原型和暴露前练习版本；第 30 节记录 2026-07-26 管理员页面内详细结果查看。旧版“Participant ID”“20 次十字”“不能过夜恢复”“20 位恢复码”等描述是当时的历史状态，不代表新版本会改写或删除那些记录。

## 1. 初始需求

目标是搭建一个睡前光照研究网页，包含：

- 约五分钟的纯色视觉刺激
- 亮红、暗红、亮蓝、暗蓝四种条件
- 实验过程中随机出现黑色十字
- 参与者点击屏幕或按键证明持续注意
- 记录十字出现时间和参与者响应时间

## 2. 界面与研究流程

采用深色沉浸式研究界面作为开始页，实验刺激页保持全屏纯色，避免无关界面元素影响刺激。

v2 阶段流程（截至 2026-07-11）：

1. 输入 Participant ID
2. 选择指定条件
3. 阅读英文任务说明并确认开始
4. 三秒倒计时
5. 五分钟有效实验时间的全屏刺激与注意力任务
6. 结果统计与 CSV/JSON 导出

英文说明会在实验开始前明确告知参与者保持专注、等待黑色十字，并在十字出现时按 `Space` 或点击屏幕；同时说明 `P` 暂停/继续和依次输入 `E`、`N`、`D` 提前终止的控制方式。

## 3. 实验引擎实现

初版使用连续随机延迟安排十字。代码审查发现，如果下一次十字从参与者响应后才开始计时，反应较快的人会得到更多试次，影响条件可比性。

v2 随后改为：

- 每次会话开始时预先生成固定 `20` 次试次（该设计在 v3 中被四次稀疏十字取代，但历史 v2 数据保持不变）
- 将 290 秒可用区间划分为 20 个 14.5 秒时间窗，在每个窗口的 25%–75% 区间随机选择计划出现时间
- 所有计划与参与者的点击速度独立
- 十字坐标也在会话开始时预先生成并写入计划

## 4. 计时精度改进

初版在 React 更新界面前记录出现时间，可能让记录时间早于真实绘制时间。

改进后：

- 先同步提交十字界面
- 再在浏览器下一次 `requestAnimationFrame` 中记录视觉 onset
- 保存计划 onset 和实际 onset
- 反应时使用实际 onset 计算
- 持久化操作延后到绘制之后，减少对显示时机的影响

## 5. 注意力与环境记录

除命中和漏答外，增加了以下记录：

- 无目标时的误点
- 页面隐藏和恢复
- 进入和退出全屏
- 全屏请求失败
- 指针坐标、十字坐标和输入方式

如果浏览器隐藏或计时器被节流，已经错过显示窗口的试次不会连续补播，而会标记为 `omitted`。v2 正常完成时会补全所有未能呈现的计划试次，确保完整会话保留 20 次结果；使用 `END` 提前终止时，未来试次只保留在 `trialPlan`，不会被误算为漏答或 `omitted`，正在进行的试次会标记为 `cancelled`。

随后根据实验控制需求增加：

- 在运行时按 `P` 暂停，再按 `P` 继续
- 暂停期间停止刺激和注意力试次，并冻结五分钟有效实验倒计时
- 记录每次暂停的开始时间、结束时间和持续时间
- 在运行或暂停时依次输入 `E`、`N`、`D` 提前终止，并将会话状态记录为 `terminated`
- 正常完成的会话状态记录为 `completed`
- 刺激页左下角显示小型剩余时间倒计时
- 无十字时的点击，以及一次命中后的额外点击，统一记录为 `false_click`

## 6. 刺激可见性处理

审查发现，纯黑十字在 `#660000`、`#0000FF` 和尤其 `#000066` 上的视觉对比较低。

当前处理是保留黑色十字主体，同时增加细浅色轮廓。这样可以提高暗色条件下的可见性，但正式研究仍需要在实际设备上进行对比度与物理亮度校准。

## 7. v2 数据结构与导出

实现了：

- 会话级 JSON 记录
- 事件级 CSV 导出
- 本地浏览器自动保存
- 最终记录自动上传到 Supabase，并保留本地失败重试副本
- 新版重试队列使用 `sleep-light-study:sessions:v2`；旧版 `v1` 非 UUID 记录不会被重复提交或删除
- 通过 `admin` 保留值进入密码保护的远程数据页面
- 管理员远程搜索、分批加载全部有效记录、刷新和下载全部或单次 CSV/JSON
- CSV 事件按时间排序
- CSV 始终写入 `session_summary`，确保开始后立即 `END` 也保留完整会话元数据
- 参与者编号的电子表格公式注入防护
- 实验结束后的命中数、漏答数、平均反应时和准确率统计
- 暂停事件的开始、结束和持续时间记录
- 实际有效实验时长、墙钟总时长和暂停总时长分别记录
- 正常完成与提前终止的会话状态记录
- `false_click` 事件记录，包括无十字点击和命中后的多余点击

平均反应时只使用 `hit` 试次；准确率为 `hit / (hit + missed)`，不包含 `omitted` 与误点。

暂停不会减少预设的五分钟有效实验时间；继续后，计划试次和剩余倒计时从暂停点恢复。CSV 使用独立的暂停事件行保存暂停开始、结束和时长，JSON 保留完整会话级暂停记录。

远程端只接收 `completed` 或 `terminated` 最终记录；频繁产生的 `active` 快照仍只保存在本地。最终记录远程保存成功后会从本地重试队列清除，失败记录会在下次打开页面时再次提交。`test` 和 `admin` 均被客户端与数据库约束排除。数据库开启并强制 RLS：匿名用户只有新增权限，管理员读取权限通过 Supabase Auth 用户 UUID 的私有 allow-list 判断。

匿名写入能保护记录不被参与者读取或修改，但不能证明每条提交一定来自真实实验页面。正式研究如果需要更强的数据完整性，应升级为 Supabase Edge Function，并加入一次性 participant token、速率限制或其他防滥用措施。

## 8. 技术栈

- Next.js `16.2.6`
- React `19.2.6`
- vinext `0.0.50`
- Vite `8.0.13`
- Tailwind CSS `4.2.1`
- Node.js `>=22.13.0`
- Cloudflare Worker / OpenAI Sites 构建产物
- GitHub Actions / GitHub Pages 静态备用部署
- Supabase Postgres、Auth 与 Row Level Security

## 9. v2 质量审查与验证

完成的检查包括：

- TypeScript/React 生产构建通过
- 固定 20 次计划不受响应速度影响
- 十字实际绘制 onset 在界面提交后记录
- CSV 各类事件列对齐
- 暂停、提前终止和误点事件具有可区分的数据状态
- 五分钟计时使用有效实验时间，暂停期间冻结
- 页面隐藏导致的过期试次不会被快速补播
- 只接受主指针输入，避免多点触摸污染
- GitHub Pages 页面加载无控制台错误
- Participant ID 输入和条件单选控件已实际验证
- 自动测试覆盖了“第一个十字前立即 `END`”的 CSV 导出边界
- 7 项自动测试覆盖隐藏 test 提示、单一 CSV 表头、管理员分支顺序、Supabase RLS、远程嵌套数据校验、500/501 分页、损坏响应和后续页失败

## 10. 部署历史

### OpenAI Sites

首次部署地址：

<https://sleep-light-study.dkm26355.chatgpt.site/>

网站随后调整为公开访问，但部分网络仍被站点前的 Cloudflare 安全层返回 HTTP 403。该问题发生在网站代码执行之前，不能通过修改页面访问权限解决。

### GitHub Pages

为避免 `chatgpt.site` 的网络拦截，安装并授权 GitHub 连接，创建公开仓库：

<https://github.com/HowToBeAWafer/sleep-light-study>

随后：

1. 生成可独立运行的静态页面和客户端资源
2. 将资源上传到 GitHub 仓库
3. 添加 GitHub Pages Actions 工作流
4. 在仓库设置中启用 GitHub Actions 作为 Pages 来源
5. 重新执行发布并确认成功
6. 在浏览器中验证页面与交互控件

最终推荐地址：

<https://howtobeawafer.github.io/sleep-light-study/>

## 11. 当时的后续建议（v2）

- 加入伦理审批后的知情同意页面
- 在高风险或公开招募场景中增加一次性参与者代码和服务器端限流
- 保存设备、浏览器、屏幕尺寸和校准编号
- 使用测光设备确定实际亮度，而不是只依赖 RGB 数值
- 增加测试模式，以较短时长快速检查完整实验流程
- 在正式收集前执行预实验和跨设备一致性检查

## 12. v2 阶段的研究解释限制

- 浏览器无法记录实际 lux、光谱功率分布、环境光或显示器校准状态。
- 当前版本不记录设备型号、浏览器版本、刷新率或窗口尺寸。
- 条件由操作员手选，没有内置随机分组、条件平衡、盲法或重复 Participant ID 检查。
- 刷新、关闭、休眠后不会续跑正在进行的实验。
- `localStorage` 可能被用户清理或在隐私模式下受限，完成后必须及时下载。
- 匿名数据库新增接口可被伪造或滥用；RLS 保护记录机密性，但不能独立证明研究真实性。
- 本任务只记录短时视觉注意反应，不记录入睡时间、睡眠质量或其他睡眠结局。

## 13. 内置 Test mode

根据试用需求，加入保留 Participant ID `test`（不区分大小写）：

- 可反复运行完整五分钟实验
- 所有计时、十字和结果统计仍正常工作
- 暂停/继续、`END` 提前终止和左下角倒计时仍正常工作
- `persist()` 在检测到 test participant 后直接返回内存记录，不写入 `localStorage`
- 历史会话导出会过滤 test participant
- 结果页隐藏 CSV/JSON 下载，并明确说明记录未保存
- 进入页面时会清理旧版本可能留下的 test participant 本地记录
- Participant ID 输入框不公开提示 `test`，避免参与者得知研究者测试入口

## 14. 触摸设备控制

为解决手机和 iPad 不便调出键盘的问题，网页现在检测输入能力而不是解析设备名称：`navigator.maxTouchPoints`、`(pointer: coarse)` 和真实 `pointerType="touch"` 任一满足，就启用触摸模式。这样即使 iPad 使用桌面浏览器标识，也能显示正确界面；带触摸屏的电脑显示额外按钮属于安全降级，键盘快捷键仍然保留。

触摸模式的英文教程只说明点击屏幕和底部控件。实验期间显示 Pause/Resume 与 End；End 第一次点击只进入三秒确认状态，第二次点击才终止，终止原因保存为 `touch_end`。控制栏与整屏反应区域分离，并阻止 pointer 事件冒泡，因此操作按钮不会被误记为命中或 `false_click`。所有设备的十字纵向位置统一限制在 15%–60%，包括较矮的横屏设备，并保持各设备使用相同的位置分布。

教程页同时提供低调的手动模式切换，解决触屏笔记本等混合设备可能更适合键盘操作的情况；参与者手动选择后，后续触摸事件不会覆盖该选择。暂停、恢复、结束和阶段切换都会清除未完成的 `E N D` 序列，避免跨暂停继续旧指令。

## 15. Protocol v3：降低十字频率

研究设计复核后认为，五分钟内出现 20 次目标可能使参与者持续保持过高警觉，反而改变睡前状态。v3 因此新增 `attentionProtocolVersion: "sparse-4-50-70-v1"`：

- 每个颜色条件预生成固定 `4` 次黑色十字，不超过四次
- 相邻计划 onset 间隔为 `50–70` 秒
- 十字仍显示 `1,800 ms`，并继续记录计划 onset、实际绘制 onset、点击和误点
- 四次计划不受参与者回应速度影响
- v2 的 20 次历史记录不迁移、不重写，数据库同时接受 schema 2 和 schema 3

Control 没有颜色曝光，因此 `trialPlan` 和 `trials` 都严格为空。分析时必须按 `schemaVersion` 和 `attentionProtocolVersion` 区分旧版高频任务与新版稀疏任务。

## 16. 第五个条件：Control — Normal Sleep

v3 把 Control 作为与四种颜色并列的第五个正式条件。它表示整晚按照平常方式睡眠，而不是黑色、暗色或“零亮度视频”：

- 不进入五分钟屏幕颜色曝光
- 不改变网页亮度
- 不呈现十字注意力任务
- 仍完成睡前问卷、睡前 KSS、整夜等待、睡醒后 KSS 和反应检查

Control 会话保存 `plannedDurationMs = 0`、`exposureStatus = "not-applicable"` 和空注意力数组。网页不强制 washout day；多个条件之间是否间隔、间隔多久以及条件顺序由研究者的预注册方案决定。

## 17. 睡前/睡醒问卷与设备记录

所有五种条件在睡前使用同一份版本化问卷。问卷包括：

- 最近一次睡眠时尝试入睡的时间
- 完整标注版 KSS 1–9（回顾刚刚过去五分钟）
- 会话前两小时是否使用电子屏幕以及估计分钟数
- 今晚是否开灯睡觉以及灯光颜色类别
- 睡眠环境主观温度
- 是否使用助眠药物或保健品；只记录 Yes / No / Prefer not to answer，不收集药名
- 最近醒来时的休息恢复感 1–5 和最近睡眠质量 1–5
- 过去八小时咖啡因、入睡音乐、环境噪音等级和过去十二小时剧烈运动

行为和环境项目作为独立协变量保存，不合成为未经验证的总分。睡醒后再次使用同一 KSS 1–9，并特意放在反应测试说明之前。KSS 的 1–9 英文标签完整保存在 `app/protocol-v3.ts`，问卷分别带有 `pre-study-v1` 与 `post-study-v1` 版本标识。

网页不保存 User-Agent。它根据触点数量、粗/细指针、悬停能力和屏幕短边自动分类 Phone、Tablet 或 Computer，参与者可以修正。睡前和睡醒后各保存一次设备信息，并记录 `deviceChanged`，供分析设备变化造成的计时差异。

## 18. 睡醒后的三次反应检查

睡醒后 KSS 完成后，参与者会先看到“放松并自然回应”的说明。反应流程包括一次练习，并继续到取得三次有效正式回应：

- 每次目标等待时间随机为 `2–5` 秒
- 目标出现后的回应窗口为 `2` 秒
- 提前回应记为 `false-start`
- 超时记为 `missed`
- 提前回应和漏答分别累计计数，然后重做当前编号，不会被当成有效回应
- 最终只使用三次 `valid` 回应计算平均值和中位数；同时保存 `false-start` 与 `missed` 累计数量

该模块标识为 `relaxed-reaction-test-v1`，属于简短浏览器反应检查，不应描述成完整或临床验证的 PVT。绝对毫秒值仍会受到设备与输入方式延迟影响。

## 19. 48 小时受保护草稿与恢复

v3 跨越一整晚，不能再沿用 v2 “最终结束后才上传”的单阶段假设。新增过夜草稿协议 `overnight-v1`：

- 浏览器生成 32 字节随机恢复令牌并持有其 64 位十六进制原文
- Supabase 使用 PostgreSQL 核心 SHA-256 函数保存令牌摘要，不依赖 `pgcrypto`
- 草稿存于 `private.study_drafts`，匿名和普通已登录角色均无表级权限
- 匿名页面只能执行三个最小权限 RPC：保存、按令牌读取和删除自己的草稿
- 草稿必须符合 active `overnight-v1` schema v3 结构且不超过 `128 KiB`
- 睡前问卷后保存 `not-started` 草稿；颜色阶段使用 `in-progress` 检查点记录试次、回应、暂停和环境事件，意外刷新则保存为 `page_reload` 提前中断
- 每次保存后有效期更新为 48 小时；读取会拒绝并清理已过期草稿
- 睡眠标记、睡醒返回和睡醒后问卷都必须先成功写入同一浏览器，页面才进入下一阶段；正式会话另设四小时防误点门槛
- 完成睡醒后问卷和反应检查、成功形成最终记录后删除草稿

恢复能力依赖参与者仍持有 bearer token；Participant ID 本身不能用于查询草稿。该保护机制不是客户端端到端加密，文档和同意材料不能把它描述成“加密问卷”。

## 20. Supabase v3 兼容迁移

全新项目可运行更新后的 `supabase/setup.sql`。已经运行 v2 的项目使用独立迁移文件 `supabase/migrations/20260718_protocol_v3.sql`：

- 保留所有 v2 行和原有管理员 allow-list
- 为最终会话增加 `control` 和 schema v3 约束
- v3 颜色条件要求四次计划，Control 要求零次计划
- v3 最终 payload 要求设备、前测、后测和反应检查对象
- 创建私有草稿表、PostgreSQL 核心 SHA-256 token 支持和三个受限 RPC

迁移成功时 SQL Editor 正常显示 `Success. No rows returned`。上线顺序应是：恢复/确认 Supabase 项目健康、运行迁移、完成端到端测试、再发布 v3 网页；否则新网页提交会被旧数据库的四条件/20 次约束拒绝。

## 21. 中英双语界面与完整参与者教程

新版本在首页加入 English / 中文切换，并让所选语言贯穿姓名档案、设备确认、教程、睡前问卷、颜色/Control 阶段、睡醒流程、KSS、反应检查、结果和反馈。教程不只说明按键，还覆盖整个实验过程：

1. 每次实验尽量使用相同设备和显示设置，并保持相近的温度、计划入睡时间、声音/噪音、灯光、睡前屏幕使用、音乐、咖啡因和助眠品习惯。
2. 一致性要求不能取代诚实回答；参与者不应为了“答案相同”而报告虚假环境，也不应为了研究留在不安全或不舒适的环境。
3. 颜色条件中保持注视，十字出现时点击/轻触或按 `Space` / `Enter`；电脑端保留 `P` 和 `E N D`，触摸端使用底部控制。
4. Control 跳过颜色、亮度和十字阶段，按平常方式完成整晚睡眠。
5. 睡醒后返回同一网页，完成第二次 KSS、一次练习和三次有效自然反应，再选择提交或跳过反馈。

英文教程表达同样的研究要求，不是中文内容的简化版。两种语言切换只改变呈现语言，不改变条件、计时、问卷字段或记录结构。

## 22. 唯一实验姓名、隐私和恢复码

开始页的 `Participant ID` 改为 `Study name / 实验姓名`。参与者可以使用真名或网名；为减少不必要的身份暴露，界面明确推荐使用不会直接识别本人、也不包含邮箱或学校编号的网名。

- 姓名经 NFKC 规范化、首尾清理、内部 Unicode 空白合并和大小写不敏感比较后保持唯一。
- 首次建立姓名档案时生成 20 位恢复码；原始恢复码保留在参与者浏览器或由参与者自行私下保存。
- 浏览器把恢复码转换为 SHA-256 proof 后发送；数据库再哈希该 proof，不保存可显示的原始恢复码。
- 返回者可使用原浏览器，或在其他设备输入相同姓名和恢复码，重新打开同一个档案。
- 恢复码无法由管理员反向取回，参与者不应分享；它用于证明对该姓名历史的访问权，而不是研究者可重置的普通密码。

唯一姓名不能保证其一定是真实身份，也不应被当作身份验证。它的目的只是让每个人的多晚记录整洁地归入同一研究档案。

## 23. 五条件历史与只追加反馈

姓名档案显示 Bright Red、Dim Red、Bright Blue、Dim Blue 和 Control 的完成/剩余状态，并保留每次完成的会话历史。网页只汇总进度，不自动分配下一个条件、不随机化顺序，也不阻止连续晚实验；顺序继续由研究方案决定。重复完成同一条件会新增一条会话，不会覆盖前一次。

每次完整实验后显示可选的 Feedback / Question 窗口：

- 参与者可选择反馈或问题、输入最多 4,000 字符并提交，也可以跳过。
- 每次提交都生成独立 ID，并保存时间、界面语言、提示版本和网站构建版本。
- 反馈是 append-only；新的留言不会更新或替换旧留言。
- 管理员可以按姓名和会话查看所有历史留言，但普通参与者不能枚举其他人的留言。

## 24. 管理员黄色环境一致性提醒

管理员仪表板按唯一姓名汇总已完成的 v3 会话，并在以下任一条件成立时显示黄色感叹号，提示研究者人工复核：

- 用 24 小时圆周算法处理跨午夜后，报告的入睡时间最小跨度超过 90 分钟；
- 主观温度从最低到最高相差超过一个有序等级；
- 环境噪音从最低到最高相差超过一个有序等级；
- 是否开灯睡觉，或开灯时的灯光颜色发生变化；
- 屏幕使用、入睡音乐、过去八小时咖啡因、助眠品四项中至少两项出现 Yes/No 变化。

`Prefer not to answer` 不用于制造变化。研究者分配的颜色/Control 条件变化是实验自变量，**永远不会**触发环境不一致提醒。黄色感叹号只表示“需要认真查看”，不会自动排除、删除或更改会话。

## 25. 构建版本和历史记录保留

本版本为每个新会话写入 `studyBuildVersion`；反馈另保存提示版本和构建版本。这样导出和管理员页面可以把每份答案追溯到参与者实际使用的网站构建，而不是假设所有 v3 记录都来自相同界面。

历史策略为只追加与增量升级：

- 旧 schema v2、早期 schema v3、每次问卷答案、条件结果和反馈都保留原值；
- 新版本不会把旧答案转换成新字段，不会用同姓名的新会话覆盖旧会话，也不会自动删除旧记录；
- 新迁移为现有表增加档案关联、反馈和 append-only 保护，而不是重建或清空 `study_sessions`；
- 不同问卷、提示、协议和网页构建必须依靠其版本字段分层分析。

2026-07-18 上线时，生产项目先运行 Protocol v3 基础迁移，再运行档案迁移。首次尝试在 SQL 解析阶段发现数组长度约束的结尾括号位置错误；该事务未执行任何数据库变更。修正 `20260718_protocol_v3.sql` 与 `setup.sql` 的同一约束后，两份完整事务均返回 `Success. No rows returned`。迁移前后 `study_sessions` 都是 0 条；三张档案/反馈表、七个公开 RPC、六条 v3 约束、三个草稿 RPC、只追加触发器和管理员 allow-list 随后全部通过核对。匹配的网页构建发布到 GitHub Pages，GitHub Actions #40 成功，英文、中文、五条件和不保存数据的 `test` 教程流程通过线上冒烟测试。

同一次发布也准备了 OpenAI Sites 构建包，但受限执行环境无法连接该站点的源码仓库；公开 `chatgpt.site` 地址复核时仍返回 Cloudflare “Sorry, you have been blocked”。因此没有用旧源码提交号冒充新版，也没有把该备用地址记录为本版发布成功。GitHub Pages 是本版唯一参与者入口。

## 26. 参与者密码账户与刷新后恢复

2026-07-23 源码把新参与者的长期档案凭证由系统生成的恢复码改为参与者自己选择的密码：

- 新账户使用全局唯一的真名或网名，以及 `8–128` 个字符的密码；
- 浏览器以姓名域分离的盐和 PBKDF2-HMAC-SHA256 派生固定长度凭证证明，原始密码不发送到 PostgreSQL；
- 同一浏览器只记住已登录档案所需的凭证证明，刷新页面后自动恢复该档案及已完成/待完成条件；
- 新浏览器或新设备可用相同姓名和密码登录并读取同一进度；
- 参与者账户沿用私有姓名档案和最小权限 RPC，不创建 Supabase Auth 用户；Supabase Email Auth 仍只服务于管理员；
- 当前参与者网页不提供邮箱字段或邮件重置；参与者也不应把邮箱用作实验姓名或写入自由文本反馈。

为保留 2026-07-18 已建档案，新增 `20260723_password_accounts.sql`。其中 `upgrade_participant_profile_credential` 先核对旧恢复码证明，再只替换档案凭证哈希；历史会话、问卷答案、条件进度、反馈和构建版本都不更新或删除。旧本地档案仍可进入一次性升级流程，升级成功后改用密码登录。

## 27. 未上线并已取消的一次性晨间提醒原型

2026-07-23 曾在本地源码中实现自愿的一次性晨间提醒原型：开始页只有明确选择 Yes 才显示邮箱输入，并说明邮箱仅用于约八小时后的这一封提醒。

隐私和发送设计包括：

- `20260723_morning_reminders.sql` 创建独立的私有 `private.morning_reminders` 队列；邮箱不写入参与者档案、隔夜草稿 payload、最终研究记录、问卷、反馈或管理员研究数据导出；
- `schedule_morning_reminder` 同时核对档案凭证和已经标记睡眠的真实未过期草稿，避免只凭随机会话 UUID 安排邮件；
- 同一档案同一时间只允许一个活动提醒，并限制短时间内重复请求；
- 项目总量另限制为每小时最多 30 个、每 24 小时最多 100 个新提醒请求，并使用事务锁避免并发绕过；这些上限只能限制损害，不能替代公开招募所需的邀请码、CAPTCHA/Turnstile 或边缘 IP 限流；
- `cancel_morning_reminder` 允许参与者在邮件尚未领取发送前取消；
- `claim_due_morning_reminders` 和 `complete_morning_reminder` 只授权 `service_role`，使用有限批量、领取租约、重试和过期窗口；
- 发送成功、最终失败或取消都会在同一终止更新中清空邮箱地址；
- `send-morning-reminders` Edge Function 使用 Resend、供应商幂等键、短请求超时和中英文邮件正文，不把邮箱写入错误正文。

该原型从未发布到生产网页，`20260723_morning_reminders.sql` 从未在生产项目执行，发送函数、Resend secrets 和 Cron 也从未部署，因此没有通过该原型收集生产参与者邮箱。研究者随后在正式发布前取消邮件提醒需求；当前源码删除了邮箱界面、排程/取消 RPC 客户端、数据库迁移和发送函数，并在新版首次运行时清除可能由本地预览留下的旧浏览器邮箱键。本节保留设计历史，不代表当前功能。

## 28. 教程关键词和暴露前引导练习

教程中影响操作和研究一致性的关键词改为粗体，包括五分钟、黑色十字、回应按键/点击、暂停/继续、`END`、整晚正常睡眠、同一浏览器、分配日期和条件顺序。加粗只改变可读性，不改变协议数据结构。

四种颜色条件在正式睡前问卷和光照前新增一个隔离练习轮：

1. 先说明等待十字和回应方式；
2. 在低亮度暗色中性背景呈现练习十字并反馈是否正确回应；
3. 引导参与者练习暂停/继续；
4. 引导电脑端使用 `E N D` 或触摸端使用 End 控件；
5. 完成后进入正式睡前流程。

练习只存在于当前 React 状态，不调用网络、不写入本地研究记录，也不会混入正式注意力、误点、暂停或反应时数据。Control 跳过这一练习，以免给“无颜色、无亮度、无十字任务”的对照条件增加额外屏幕暴露。睡醒后反应检查原有的不计分练习仍然保留，两次练习用途不同。即使采用暗色背景，该步骤仍会增加少量且受操作速度影响的屏幕时间；正式方案应把它明确列为颜色条件的标准化训练步骤，并在预实验中评估潜在影响。

## 29. 2026-07-23 密码版本验证与发布状态

账户派生/本地档案和独立练习轮均加入针对性自动测试，匹配网页源码完成本地生产构建检查。未发布原型曾使用 `2026-07-23-password-practice-reminders-v1`；取消邮件功能后的中间构建标识为 `2026-07-23-password-practice-v1`。加入管理员详细结果查看后的待发布构建标识为 `2026-07-26-password-practice-admin-results-v1`。文档同时复核了密码迁移、鲜明的 Control 分支和旧恢复码升级路径。

截至 2026-07-26 的状态为：

- 项目负责人确认完整 `20260723_password_accounts.sql` 已在生产项目成功执行，SQL Editor 显示 `Success. No rows returned`；
- 邮件提醒原型从未执行生产迁移或配置发送服务，并已从当前源码移除；
- 匹配的 `2026-07-26-password-practice-admin-results-v1` GitHub Pages / OpenAI Sites 网页构建和发布后端到端核对仍未在本日志中记为完成。

上线前仍必须按 `SUPABASE_SETUP.md` 复核密码升级函数和权限，部署匹配网页，并完成账户建立、刷新恢复、新浏览器登录、旧恢复码升级、颜色练习、Control 跳过练习、完整过夜流程、最终保存、管理员页面内分类详情及文件下载的端到端测试。还应确认生产环境没有遗留的提醒表、RPC、Edge Function、Cron 或邮件 secrets。旧版公开网页和旧数据在此之前保持原状。

## 30. 2026-07-26 管理员页面内详细结果查看

管理员仪表板原本只在摘要表中显示少量指标，研究者必须下载 CSV/JSON 才能检查完整会话。新版本保留摘要表和所有下载功能，并为每条会话加入只读的 **View details / 查看详情** 展开区域。一次只展开一条会话，内容按研究审查顺序组织为：

1. 会话、姓名档案、schema、协议、问卷、构建版本和精确关联方式；
2. 条件、颜色/亮度暴露、开始/结束/睡眠/返回/完成时间和精确时长；
3. 睡前问卷全部原始答案，以及前后 KSS 分值和完整量表标签；
4. 睡前/睡醒后设备与设备变化；
5. 注意力计划/试次、无目标或额外点击、暂停和页面/全屏事件；
6. 三次正式反应、平均/中位反应时、提前响应和漏答；
7. 反馈/问题、姓名历史和黄色一致性复核；
8. 仅按需生成并显示的完整、已校验 JSON payload。

大型试次和事件数组每页显示 50 条，避免一次渲染最多数千条误点或事件。详情保留精确 ISO 时间、毫秒值、序号和 ID，不只显示摘要中经过格式化或四舍五入的值。档案优先用 `participantProfileId` 精确关联；只对没有该字段的历史记录使用规范化姓名回退，并把关联方式显示给管理员。

这是现有管理员数据读取结果的客户端整理，不增加数据库写入，也不需要新的 SQL、表、RPC、grant 或 RLS policy。详情按钮不能修改、排除或删除记录。参与者反馈和完整 JSON 都由 React 作为普通转义文本渲染，不使用 HTML 注入。schema v2 从未采集的 v3 问卷、设备、反应检查和姓名档案字段明确标为 **Not collected in schema v2 / schema v2 未收集**，不会推断、补写或把旧记录伪装成 v3。单次和全部 CSV/JSON 下载继续保留，供离线复核和正式分析。

## 变更摘要

| 日期 | 变更 |
| --- | --- |
| 2026-07-10 | 建立研究页面和四种光照条件 |
| 2026-07-10 | 加入五分钟全屏注意力任务和本地数据导出 |
| 2026-07-10 | 改为固定 20 次预生成试次并修正 onset 计时 |
| 2026-07-10 | 增加环境事件、误点、漏答和 omitted 记录 |
| 2026-07-10 | 完成 OpenAI Sites 部署并确认 Cloudflare 网络拦截 |
| 2026-07-10 | 迁移 GitHub Pages，发布并验证公开备用站点 |
| 2026-07-10 | 加入可重复使用且不保存记录的内置 test participant |
| 2026-07-10 | 加入英文说明页、P 暂停/继续、END 提前终止、暂停日志、误点日志和左下角倒计时 |
| 2026-07-10 | 从参与者开始页隐藏 test 保留值 |
| 2026-07-11 | 本地接入 Supabase 最终记录上传、UUID 管理员 allow-list 和密码保护的数据仪表板；等待完成 Supabase 一次性设置后发布 |
| 2026-07-11 | 完成 Supabase 数据库脚本；明确保留 Email provider，并仅关闭公开新用户注册，避免管理员密码登录失效 |
| 2026-07-11 | 将 Supabase 远程保存、管理员仪表板、全屏兼容修复和完整设置文档发布到 GitHub Pages |
| 2026-07-11 | 自动检测触摸能力；为手机和 iPad 加入专用教程、Pause/Resume 控件与二次确认 End 按钮 |
| 2026-07-18 | 建立 Protocol v3：四次 50–70 秒稀疏十字、正常睡眠 Control、前后 KSS、睡前协变量问卷、前后设备记录、三次反应检查和 48 小时受保护草稿恢复 |
| 2026-07-18 | 新增兼容 v2 历史数据的 `20260718_protocol_v3.sql` 迁移；网页不强制 washout day，条件间隔继续由研究方案管理 |
| 2026-07-18 | 完成中英双语全流程教程、唯一真名/网名档案、20 位恢复码、每姓名五条件历史和可选反馈/问题源码 |
| 2026-07-18 | 加入管理员黄色环境一致性提醒及 `studyBuildVersion` 追踪；分配的实验颜色变化明确不触发提醒 |
| 2026-07-18 | 生产项目依次完成 Protocol v3 与档案增量迁移；迁移前后会话均为 0 条，全部预期数据库对象和管理员 allow-list 核对通过 |
| 2026-07-18 | 修正迁移脚本数组长度约束的括号位置，并将 GitHub Pages 工作流改为在 Ubuntu 构建后上传正确的静态导出目录 |
| 2026-07-18 | 发布中英双语姓名档案版本到 GitHub Pages，并完成中英文与隐藏 test 模式线上冒烟测试；OpenAI Sites 因源码仓库网络隔离及 Cloudflare 拦截保留旧版，未记为发布成功 |
| 2026-07-23 | 本地实现唯一姓名 + 8–128 字符密码账户、同浏览器刷新自动恢复、新浏览器登录和旧恢复码无损升级 |
| 2026-07-23 | 本地实现一次性晨间邮件提醒原型；该原型从未上线或收集生产邮箱，随后在正式发布前取消并从当前源码移除 |
| 2026-07-23 | 教程关键操作词加粗；四种颜色条件新增独立不保存引导练习，Control 为避免额外暴露而跳过 |
| 2026-07-23 | 无邮件版源码和针对性自动检查在本地完成；当时密码迁移和匹配网页部署仍为 pending |
| 2026-07-26 | 项目负责人确认生产项目成功执行 `20260723_password_accounts.sql`；匹配网页部署和端到端核对仍作为独立发布步骤 |
| 2026-07-26 | 构建版本更新为 `2026-07-26-password-practice-admin-results-v1`；管理员页面新增分类、分页、只读详细结果查看，保留全部 CSV/JSON 下载且不改变 SQL/RLS |
